{
  "name": "mediquerium-registration-admin",
  "version": "1.1.0",
  "private": true,
  "description": "Mediquerium registration site with per-day/cohort limits, JSON persistence, admin dashboard, and email confirmations.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "nodemailer": "^6.9.13"
  }
}